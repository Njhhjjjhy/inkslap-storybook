{
  "name": "inkslap-storybook",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "Storybook site for the moha-presentation design system used in the InkSlap platform proposal.",
  "scripts": {
    "storybook": "storybook dev -p 6006",
    "build-storybook": "storybook build",
    "deploy": "gh auth switch -u RiaanMOHA && git push origin main && gh auth switch -u Njhhjjjhy && git push vercel main && gh auth switch -u RiaanMOHA"
  },
  "devDependencies": {
    "@chromatic-com/storybook": "^5.0.0",
    "@storybook/addon-a11y": "^10.2.1",
    "@storybook/addon-docs": "^10.2.1",
    "@storybook/react-vite": "^10.2.1",
    "storybook": "^10.2.1"
  },
  "dependencies": {
    "@mdx-js/react": "^3.1.1",
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  }
}
